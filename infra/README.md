# infra/

Terraform (`bpg/proxmox` provider) for VM provisioning + cloud-init. Manual local apply only — review `plan`, apply yourself, no CI automation.

Not started yet — see [Phase 1 in the implementation plan](../docs/implementation-plan.md#phase-1--terraform-vm-provisioning-infra).
